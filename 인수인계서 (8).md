# BNCP 통합관리 대시보드 — 인수인계서 (8)

**현재 버전 v2.52.0** · 검사 **4종** 전부 통과 · 작성 2026-08-31
(smoke 565 · vendor 204 · direct 32 · gs 62)
★검사 : `node tools/smoke.js` · `smoke_vendor.js` · `smoke_direct.js` · **`smoke_gs.js`(서버)**

> 새 창에서 **「다음 시작해」** 한 마디면 이어서 작업할 수 있도록 쓴 문서다.
> **0장(지금 당장 할 일)** 부터 읽으면 된다.
> 인수인계서 (7)은 8/22자다. **그 「남은 것」 목록은 낡았다** — 아래 0-D를 볼 것.

---

# 0. 지금 당장 할 일

## 0-A. ★서버 재배포가 **밀려 있다** (v2.48.0)

**묶음 전송**을 넣으면서 `Code.gs`를 고쳤다. **재배포해야 빨라진다.**

```
Apps Script 편집기 → [배포] → [배포 관리] → 연필(수정)
   → 버전 「새 버전」 → [배포]
★[새 배포]를 누르면 주소가 바뀐다. 절대 누르지 말 것.
```

★**재배포 안 해도 망가지지 않는다.** 서버가 `meta`에서 `batch:true`라고 말할 때만
화면이 묶음을 쓴다(`API.canBatch`). 옛 서버면 종전대로 한 줄씩 보낸다.
이 안전장치가 없으면 묶음이 `etc` 시트에 통째로 한 줄로 쌓여 **자료가 뭉개진다.**
검사 [86]이 이것을 지킨다 — 지우면 실패한다.

## 0-B. 남은 기능 **5개** (다음 작업)

~~처리시간 표시~~ → **v2.51.0에서 끝났다**(A.procH·A.ckStamp, 확인 대기 표에 칸).
~~자재 현황 업체명~~ → **v2.52.0에서 끝났다**(mVariance가 업체별로 같이 쌓는다 →
`a.cos`. 자재 표·CSV에 「업체」 칸). ★사용자가 화면을 보고 요청한 것이라 0-B에는
없던 항목이다 — 남은 5개는 그대로다.

1. **협력업체 담당구간** — 업체마다 맡은 구간
   ※`S.vend`에 칸이 없다. 자료 모양부터 정해야 한다.
2. **전화번호** — 이라크 번호 / 관리자 번호
   ※업체 담당자 번호는 이미 있다(`vend.staff`의 "grp|name|tel"). 무엇을 더 둘지 확인.
3. **주소 · 권한분리**
4. **검측화면 간격**
5. **SI · 조경 「검토만」**

~~상단 공구 자동선택~~ → **v2.50.0에서 끝났다**(설계수량 있는 곳만·한 곳뿐이면 자동).

★**착수 전에 사용자에게 하나씩 확인할 것. 이름만으로는 화면이 안 그려진다.**
  실제로 v2.49.0에서 이름만 보고 만들었다가 제목이 네 겹이 돼 다시 했다(1-G).

## 0-C. 사용자 확인 대기

- v2.47.0 이후 **PC·모바일이 정말 같은지** (명부·설계수량·진행률·자재 설계수량)
- v2.48.0 재배포 뒤 **눈에 띄게 빨라졌는지**
- **지운 업체가 다른 기기에서도 사라지는지** (되살아나지 않는지)
- v2.50.0 — 작업량이 위치별로 갈리는지, 진행률 선택이 되는지,
  **상단 공구에 설계수량 올린 곳만 뜨는지**, 업체 화면도 그런지
- v2.52.0 — 자재 현황 「업체」 칸에 **실제 올린 업체가 제대로 뜨는지**
  (담당자 이름으로 올라온 옛 줄이 회사명으로 묶이는지)

## 0-D. ★인수인계서 (7)에서 **이미 끝난** 것 — 다시 파지 말 것

코드로 확인했다. (7)의 「남은 것」에 그대로 적혀 있어 다음 사람이 헛일하기 쉽다.

| (7)의 항목 | 실제 |
|---|---|
| 0-Z-3 구글 시트 종류별 가르기 | **끝났다** — `TYPE_SHEET`·`splitRun()` 있음 |
| 0-Z-4 용량 80% 경고 | **끝났다** — `meta`의 `cellPct`, 화면 `scap_w` |
| 0-I 장비 보유를 업체 축으로 | **끝났다** — v2.19.14, `eqPut`이 업체로 자리를 가른다 |
| 비밀번호 `prompt()` 노출 | **끝났다** — 비밀번호는 `prompt()`를 안 쓴다 |

★**0-Z-2-B 테스트 자료 일괄 삭제는 아직 미결이다.** 사용자가 「나중에 생각하자」로
보류했다. **먼저 꺼내지 말 것.**

---

# 1. 동기화 — 이 앱에서 가장 많이 다친 곳

★**v2.45~v2.48이 전부 여기다. 손대기 전에 이 장을 다 읽을 것.**

## 1-A. 구조

```
화면(localStorage)  ──보내기──▶  Google 시트(종류별)  ──받기──▶  다른 기기
```
- 보내기 : `api.send(type,row)` / **`api.sendMany(rows)`(묶음, v2.48.0)**
- 받기 : `api.changed()` — **type 없이** 부른다. 모르는 종류는 `etc` 시트에
  원문(JSON)으로 쌓였다가 그대로 되돌아온다.
  ★**그래서 새 종류를 추가해도 `Code.gs` 재배포가 필요 없다** (`vend`·`stop`·
  `staff`·`issue`·`alias`·`stock`·`mt`·`fac`·`mdes`가 이 길로 다닌다).
  ※묶음 전송은 예외 — **그건 서버가 알아야 하므로 재배포가 필요하다.**

## 1-B. 무엇이 언제 올라가는가 — `txCfgAll()` (tabs.js)

★**사람이 넣는 기준자료는 빠짐없이 여기 담겨야 한다.**

| 담긴 것 | 뜻 |
|---|---|
| `staff` `issue` `alias`/`alias2` `stock` `mt` | v2.46.0 |
| `vend` `plan` `fac` `mdes` | **v2.47.0** |
| `gone` | **v2.48.0 — 지운 것 표식** |

- **서명 게이트** : 상태마다 `JSON.stringify` 서명을 `S.cfgTx`에 적어 두고,
  **바뀐 것만** 올린다. 매 주기 같은 것을 다시 안 올린다.
- **`SIG_V`** : 전송 형식이 바뀌면 올린다 → 옛 서명과 안 맞아 **모든 기기가 1회
  다시 올린다.** 서버의 옛 줄을 올바른 값으로 덮는 길이다. (현재 `'v4'`)
- `syncNow`가 매 주기(1분·화면 진입·수동) 부른다 → **손대지 않아도** 올라간다.

★**이 표에 없으면 「등록하는 그 순간」에만 올라간다.** v2.47.0에서 사용자가
「설계수량·명부가 다르다」고 한 원인이 정확히 이것이었다 — 그 전에 등록해 둔
자료는 서버에 **한 번도** 올라간 적이 없었다.

## 1-C. ★불변식 — 어기면 반드시 사고가 난다

```
① 빈 값은 로컬을 덮지 않는다.   (union — 있는 키만 갱신·추가, 없는 키는 보존)
② 지우는 것은 del:1 표식으로만. del이 참일 때만 지운다.
③ 올릴 때와 지울 때의 id가 같아야 한다. 다르면 줄이 둘이 되어 되살아난다.
```

**②③을 어겼을 때 무슨 일이 났는지** — v2.46 「미등록 업체」 사고(1-D)를 볼 것.

## 1-D. ★사고 기록 — 「미등록 업체」 (v2.46.0 → v2.46.3)

**세 판에 걸쳐 엉뚱한 곳을 고쳤다. 같은 실수를 하지 말 것.**

| 판 | 무엇을 했나 | 결과 |
|---|---|---|
| v2.46.0 | 지급장비를 동기화하며 **없는 `co` 칸**을 보냈다(진짜 칸은 `by`) | 업체가 빈 줄이 생김 |
| v2.46.1 | `by`로 고쳤다 — 그런데 **빈 `by`로도 로컬을 덮게** 했다 | PC의 멀쩡한 업체가 지워짐 |
| v2.46.2 | 빈 값이 안 덮게 막았다 | 더 번지는 것만 막았다. **화면은 그대로** |
| v2.46.3 | **진짜 원인** : `coOf`가 **빈 업체**도 「미등록 업체」로 돌려줬다 | 고쳐짐 |

★교훈
1. **증상이 난 화면의 함수를 먼저 따라갈 것.** 동기화만 세 번 고치는 동안
   화면을 만드는 `coOf`를 안 봤다.
2. **「없음」과 「모름」은 다르다.** 이름이 **없는 것**과 명부에 **없는 이름**은
   다른 뜻이다. 섞으면 없는 업체가 화면에 선다.
3. **부르는 쪽이 빈 값을 어떻게 받는지 볼 것.** `rollupCo`는 `|| '—'`,
   `sbCoRow`는 `|| 미지정`, `eqRecon`은 `if (co)` — 전부 빈 값 대비가 돼 있었다.

## 1-E. ★검사가 헛돌았던 일 — 두 번 (0-J)

1. **없는 칸을 봤다.** `eqRecon` 결과의 업체 칸은 `gby`인데 `by`를 봤다.
   → 늘 통과. 화면이 읽는 칸으로 바로잡으니 고치기 전엔 실제로 실패했다.
2. **줄 서는 것을 안 셌다.** 전송이 큐를 거치는데 「보낸 것」만 세어 3건에서 멈췄다.
   → 「보낸 것 + 줄에 선 것」을 합쳐 세도록 바로잡았다.

★**검사를 쓰면 반드시 고침을 되돌려 실패하는지 볼 것(규칙 3-A).**
통과하는 검사는 아무것도 증명하지 않는다.

## 1-F. 전송 큐와 묶음 (v2.47.0 · v2.48.0)

- 서버 `doPost`는 `LockService.waitLock`으로 **쓰기를 한 줄로 세운다.**
- 설계수량은 **칸 하나가 한 줄**이라 한 현장에 수백~수천 건이다.
- 종전에는 전부 동시에 쐈다 → 뒤쪽이 잠금 대기를 넘겨 실패했고,
  **전송 실패는 무시하도록 돼 있어 조용히 사라졌다**(모바일 설계수량이 「일부만」
  맞던 원인).
- 지금 : **동시 3건**까지만, 넘치면 줄에서 기다린다. 서버가 묶음을 알면
  **한 요청에 100줄까지** 실어 보낸다(`txHold`로 다 쌓은 뒤 한 번에 —
  한 줄씩 곧바로 보내면 묶음이 한 줄짜리가 되어 뜻이 없다).

## 1-G. ★사고 기록 — 화면을 이름만 보고 만들었다 (v2.49.0 → v2.49.1)

작업현황 재구성에서 **기존 카드를 통째로 집어넣었다.** `openTaskHTML()`과 `v7()`은
**각자 자기 카드(제목 포함)를 만들어 돌려주는데**, 그것을 새 구획 안에 그대로 넣어
제목이 **네 겹**이 됐다(카드제목 → 구획제목 → 안쪽 카드제목 → 또 다른 머리).
소제목에도 축 토글용 `t_byco`(업체별)를 갖다 써서 라벨이 틀렸다.

★교훈
1. **돌려주는 것이 「표」인지 「카드」인지 보고 넣을 것.** 카드를 카드 안에 넣으면
   제목이 겹친다. 필요하면 **맨몸(bare) 갈래**를 따로 만든다(openTaskRows·
   directListHTML이 그렇게 갈라졌다).
2. **껍데기를 벗길 때 딸려 있던 것을 잃지 말 것** — 직영 기간 단추가 카드 머리에
   붙어 있었다. 소제목 줄로 옮겨 살렸다. 검사에도 넣어 뒀다.
3. 탭7(스탭)은 **종전대로 카드와 입력 폼이 있어야 한다.** 맨몸 갈래가 거기까지
   벗기면 스탭이 직영을 입력할 수 없다.

## 1-H. 화면 구성 — 지금 모습 (v2.49~v2.50)

**작업현황(탭1) 차례**
```
경고 · 확인 대기 · 재검측 · 내역서 확인필요      ← 스탭의 「내 차례」. 늘 맨 위
① 협력업체 인원 및 장비현황     ← 업체마다 인원 줄 + 장비 줄 (한 카드)
② 직영 인원 및 장비현황         ← 같은 꼴
③ 협력업체, 직영 작업현황       ← 소제목 두 개(협력업체·직영)뿐
④ 작업량                        ← ★위치별로 갈린다(Phase 1-1 / Town A·Block 3)
   작업위치(구간)
⑤ 공종별 진행률                 ← ★옆에 위치 선택. 고른 한 곳만. 합계 공정률 없음
⑥ 실측 생산성
⑦ 준비 (관리자 전용)
```
- **장비의 축은 업체 / 장비종류뿐이다** — 공종 축을 두지 말 것(v2.49.0에서 걷어냈다).
  기본은 **업체별**. `grpBy.eq`의 값은 `'co'`·`'cat'`이다(`'work'`가 아니다).
- **현장 현황(siteTable)은 지웠다** — 오른쪽 현황판과 겹쳤다.

**설계수량이 모든 것의 축이다 (v2.50.0)**
- `planLocs()` = `A.hasPlan(loc) > 0`인 위치. 상단 공구·진행률 선택·업체 위치 선택이
  **모두 이 하나를 쓴다.**
- 상단 공구는 **한 곳뿐이면 자동으로 잡힌다**(`fltAuto`). 하나도 없으면 목록 대신
  「설계수량을 먼저 올리세요」.
- **협력업체도 설계수량이 확인된 곳만 올린다**(사용자 확정, 예외 없음).
  `planPull()`이 **`rows&type=plan`만** 부른다 — ★무type 조회(`api.changed`)를 쓰면
  **다른 업체의 실적까지 내려받는다.** 절대 바꾸지 말 것.

---

# 2. 아직 안 된 것 (동기화)

- **삭제 전파는 `vend`·`staff`·`issue`·`stock`만** 한다. `plan`은 종전 규칙
  (수량 0 = 지움)을 그대로 쓰고, `alias`·`mt`·`fac`·`mdes`는 지우는 화면이 없다.
- **협력업체는 설계수량만 받는다**(v2.50.0). 나머지(명부·담당자 등)는 여전히 안 받는다 —
  필요해지면 그때도 **type을 집어서** 받을 것. 무type은 남의 실적까지 딸려 온다.
- `S.gone`은 계속 쌓인다. 아주 오래 쓰면 정리(오래된 표식 버리기)가 필요하다.
- 같은 칸을 두 기기에서 동시에 고치면 **나중에 올린 값이 남는다**(칸 단위 upsert).

---

# 3. 반드시 지킬 규칙 (요약)

- **2-A** 지레짐작하지 말 것. 시킨 것만.
- **3-A** 고칠 때마다 검사를 붙이고 **되돌려 실패하는지 확인**.
- **3-C** 문구는 ko/en/bn 세 벌(ar은 업체 화면 일부).
- **3-E** 죽은 코드를 남기지 말 것.
- **0-J** 소스 문자열이 아니라 **동작**을 검사할 것.
- **버전은 세 곳** : `assets/js/version.js` · `index.html` · `vendor.html` `?v=`
  그리고 `VERSION`에 **원인과 근거**를 적는다.
- **smoke.js는 [29]에서 표본 파일이 없어 멈춘다.** 새 검사는 **[29] 앞**에 둘 것.
  (현재 커스텀 : **[84]~[99]**)
- ★**검사가 헛도는 것을 조심할 것.** 이 판에서만 다섯 번 겪었다 :
  · 화면 전체에서 글자를 찾아 **다른 곳의 같은 글자**에 걸렸다(상단 필터의 'Phase 1-1').
  · 구획을 **제목 위치로** 잘라 뒤 카드까지 읽었고, 끝을 「다음 div.card」로 잡아
    **안쪽에 카드가 생기면 거기서 잘려** 그 카드를 못 봤다.
  · **접힌 줄**의 수량을 찾아 늘 실패했다(대분류 줄은 진행률만 보인다).
  · 토글을 **값**으로만 봐서 이름이 바뀌어도 못 잡았다.
  · **기본값을 검사 도중에** 물어 앞 검사가 눌러 둔 상태를 봤다 → 맨 위에서 붙잡아 둘 것.
  · 검사 안에서 **변수를 정의 전에** 써 파일이 중간에 죽었다(통과 건수만 보면 놓친다).
  → **반드시 고침을 되돌려 실패하는지 볼 것(3-A).** 통과하는 검사는 아무것도 증명하지 않는다.
- `tools/AppsScript_Code.gs`는 **`Code.gs`와 똑같이** 복사해 둘 것(smoke_gs가 본다).
