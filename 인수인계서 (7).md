# BNCP 통합관리 대시보드 — 인수인계서

**현재 버전 v2.22.2** · 검사 **4종** 전부 통과 · 작성 2026-08-22
★검사 : `node tools/smoke.js` · `smoke_vendor.js` · `smoke_direct.js` · **`smoke_gs.js`(서버)**

> 새 창에서 **「다음 시작해」** 한 마디면 이어서 작업할 수 있도록 쓴 문서다.
> 0장(지금 당장 할 일)부터 읽으면 된다.

---

# 0. 지금 당장 할 일

> ★**0-Z가 가장 앞이다.** 사용자와 대화로 확정했으나 **아직 코드를 손대지
> 않은** 것들이다. 그 아래 0-A부터는 종전부터 남아 있던 항목이며
> **지우지 않았다** — 0-Z가 끝나면 그리로 간다.

## 0-M. ~~측량·자재가 화면에 안 나오던 것~~ → **v2.22.2에서 끝났다**

★**진단을 두 번 잘못 짚었다. 그 과정을 남긴다 — 다음에 같은 실수를 하지 말 것.**

| 회차 | 짚은 곳 | 결과 |
|---|---|---|
| 1차 | 상단 위치 필터 | ★틀렸다. `flt`는 `{p:0,c:0}`이라 애초에 안 거르고 있었다 |
| 2차 | `todayOrOpen` 사본 (v2.22.1) | 고친 것은 맞으나 **화면과 무관**했다 |
| 3차 | 자재=플랜트 제외 · 측량=기간 단추 부재 | ★여기였다 |

★2차가 헛다리였던 이유 — `A.mreqOpen`은 **부르는 데가 한 곳도 없는 죽은
함수**였다. 실제 자재 표는 `A.matRows` → `A.mVariance` 경로다. 측량은 기간
단추가 없어 `A.dateFlt`가 늘 비어, 고친 함수가 「오늘 것만」으로 떨어졌다.

### 자재 — ★v2.17.1 결정을 사용자가 **바꿨다**
```
종전  「플랜트 자재는 목록에서 뺐다. 플랜트 신청은 시스템 밖 일이다.」(v2.17.1)
변경  「플랜트 자재도 보이게 한다」 (2026-08-22 사용자 확정)
```
옛 화면(v4Full)의 창고/플랜트 칸막이 단추를 `MAT_FLOW_ON=false`로 끄면서
**플랜트 갈래가 갈 곳을 잃었다.** 협력업체가 올린 레미콘·모래가 화면 어디에도
안 나왔다 — **올린 사람은 올렸는데 받는 사람은 못 보는 상태**였다.
· `A.matRows`가 `mVariance(f, null)`. 표에서 플랜트에는 **표(badge)**를 단다.
· ★칸막이 단추·5단계 절차는 **되살리지 않았다.** 바뀐 것은 「보이느냐」뿐이다.
· ★검사 [39]의 「플랜트 자재가 섞이지 않는다」를 바꿨다.
  **통과시키려고 고친 것이 아니라 결정이 바뀌어 검사도 바뀐 것이다.**

### 측량 — 기간 단추가 아예 없었다
`RNG_DEF`에 `surv: 'today'`가 있는데 `v3()`에 단추도 `withRng`도 없었다.
**어제 완료한 측량을 볼 방법이 아예 없었다.** `rngBtn('surv')` + `withRng('surv')`,
CSV도 같은 기간으로 감쌌다(표와 어긋나면 0-H와 같은 사고).

검사 [81]. 되돌려 6건 실패 확인.

### ★「화면에 안 보인다」 신고를 받으면 이 순서로
```
① 서버가 안 줬나   → Apps Script  auditUnpack()
② 화면이 안 받았나 → 콘솔  window.APP.S.surv.length  등
③ 화면이 안 그렸나 → ★표를 그리는 함수를 직접 따라갈 것
```
★①②가 정상이면 반드시 ③이다. **비슷한 이름의 함수를 고치고 끝냈다고 여기지 말 것** —
`A.mreqOpen`처럼 아무도 안 부르는 함수를 고치고 다 했다고 착각하기 쉽다.

## 0-K. ~~끝난 측량·자재가 화면에 안 나오던 것~~ → **v2.22.1에서 고쳤다**

★**진단 과정을 남긴다. 다음에 같은 증상이 오면 이 순서로 가를 것.**

증상 — 시트에는 측량 5건·자재 7건인데 화면에는 각 **1건만** 보였다.

가른 순서 (★셋 다 해 봐야 했다 — 앞의 둘로 잘못 짚었다)
1. **서버가 안 준 것인가** → `auditUnpack()` : 115줄 중 113줄이 조건 통과. 서버는 정상.
2. **화면이 안 받은 것인가** → 콘솔 `window.APP.S.surv.length` : **5**. 이미 다 들어와 있었다.
3. **화면이 안 그린 것인가** → 여기였다.

★위치 필터가 원인이라고 **두 번 잘못 말했다.** All Phase로 풀어도 그대로였다.
  `flt`는 `{s:'civil', p:0, c:0}`이라 애초에 아무것도 안 거르고 있었다.

원인 — **같은 뜻의 함수가 둘이었고 한쪽만 고쳐져 있었다.**
```
todayOrOpen(rec, done)      비공개 사본. ★A.dateFlt를 안 본다.
A.todayOrOpen(rec, isOpen)  공개판. v2.19.0에서 기간을 보도록 고친 것.
```
v2.19.0에서 **검측만** 공개판으로 옮기고 **측량·자재는 옛 사본에 남겨 뒀다.**
옛 사본은 「끝난 것 = 오늘 날짜만」이라, 기간을 넓혀도 8/18에 끝난 것이 안 나왔다.
★v2.19.0 주석의 「단추가 반만 듣는 셈이었다」가 **여기 그대로 남아 있었다.**

고친 것 — `A.survList`·`A.mreqOpen`을 공개판으로. ★인자 뜻이 반대다(`done` ↔ `isOpen`).
★비공개 사본은 **지웠다.** 남기면 또 한쪽만 고친다. 검사 [80]이 사본 부활을 막는다.

★**3-D의 네 번째 사례**다. 이번엔 함수 **이름이 서로 달라서**(`todayOrOpen` vs
`A.todayOrOpen`) 종전 검사에 안 걸렸다. **뜻이 같으면 이름이 달라도 위험하다.**

### ★수신 건수 「111」은 결손이 아니다
`[전체 다시 받기]`가 내는 숫자는 **새로 들어오거나 상태가 바뀐 줄**만 센 것이다
(`sync_new` = 「새로 받은 실적」). 이미 있고 안 바뀐 줄은 안 센다.
시트 115 → 받아들여짐 113 → 새로 센 것 111. **잃어버린 것이 아니다.**
★못 받는 2줄은 자재인데 **자재명(mat)이 비어 있어** `unpack`이 거른다 (0-L).

## 0-L. ★자재 2건이 자재명 없이 시트에 있다 (미착수)

`auditUnpack()`에서 `mat 7줄 → 받아들여짐 5 · 필수값없음 2`.
`unpack`은 `if (!r.mat) return null` — 자재명이 없으면 화면이 안 받는다.
시트에서 보이는 두 줄은 입력자가 `LJU3214`·`Rabies Diglah`이고 상태가 `iss`인데
**공종명·단위·수량 칸이 비어 있다.** 시범 입력 중에 생긴 것으로 보인다.
★고칠지(시트에서 자재명을 채워 넣기) 버릴지 **사용자에게 안 물었다.**

## 0-J. ★**새로 찾은 결함** — `unpack`의 `mat` 갈래가 둘이다 (미착수)

0-Z-3을 하며 서버가 보내는 종류를 세다가 발견했다. **아직 안 고쳤다 — 지시를 안 받았다.**

`assets/js/tabs.js`의 `unpack()`에 `if (r.type === 'mat')`가 **두 번** 있다.
```
323행  첫째 갈래 — 여기서 return 한다.   ← 늘 이것만 돈다
356행  둘째 갈래 — v2.20.0에서 넣은 것.  ← ★영영 안 돈다
```
첫째가 `if (!r.mat) return null;`로 끊고, 아니면 그 자리에서 `return` 하므로
**둘째로는 절대 못 간다.**

무엇이 없어지나 — 둘째 갈래에만 있는 것들이다.
`fst`(결재 단계) · `fat` · `fby` · `fwhy` · `okS` · `okV`
→ 스탭이 검토·확인한 자재 신청이 **다른 PC에서는 안 눌린 채로 보인다.**
  결재는 여러 사람이 이어서 누르는 것이라 이대로면 흐름이 성립하지 않는다.
★v2.20.0에서 「확인 처리가 서버로 안 가 되살아나던 것」과 **같은 종류의 사고**다.
보내는 쪽(`tabs.js` 485행)은 이미 `fst`·`okS`·`okV`를 **보내고 있다.** 받는 쪽만 못 받는다.

★**3-D(같은 이름 두 번 정의)의 세 번째 사례**다. 함수 이름이 아니라 **갈래**라서
지금 검사(smoke [31]·[54])에 안 걸렸다.

고칠 때 정할 것 — 둘을 합칠 것인가, 첫째를 지울 것인가. 둘째가 더 온전하다.
★같이 걸 검사 : 결재 단계를 담은 `mat` 줄을 `unpack`에 넣어 `fst`·`okS`가
살아 나오는지. 지금 넣으면 **실패한다.**

## 0-Z. 저장·초기화 — **0-Z-1·0-Z-2·0-Z-3 완료 (v2.22.0)** · 0-Z-2-B 미결

사용자와 나눈 이야기를 그대로 옮긴다. **하나도 코드에 반영되지 않았다.**

### 0-Z-1 · 0-Z-2. ~~[초기화] 비밀번호 · 문구~~ → **v2.21.1에서 끝났다**

두 건을 한 판에 넣었다. 같은 단추를 손대는 일이라 갈라 놓을 이유가 없었다.

### 지우는 범위 — ★사용자 확정으로 **바뀌었다**

사용자 확정 : 「협력업체에서 입력한것만 날리는 거야」
→ 문구를 실제에 맞추는 것이 아니라 **동작을 문구에 맞췄다.**
같이 지울 것을 따로 물었고 답은 **「없다 — 협력업체 입력 5종만」**이었다.

```
지운다  work 실적 · crew 인원장비 · insp 검측 · surv 측량 · mreq 자재신청
        (A.WIPE_BOX — core.js)
남긴다  plan 설계수량 · vend 명부 · alias/alias2 별칭 · 마스터 · lang · tab
        direct 직영 · issue 지급대장 · stock 재고 · boq 확인필요 목록
```
★뒤 넷은 **우리 쪽에서 넣은 것**이라 남긴다. `boq`는 [목록 지우기]가 따로 있다(0-B-2).
★`rxLast`는 일부러 안 건드린다. 되돌리면 증분 수신이 방금 지운 줄을 서버에서
  그대로 다시 받아 와 **초기화가 없던 일이 된다.**
★저장소 키를 통째로 지우지 않는다 — `removeItem(KEY)` 길은 없어졌다.

### 관문

```
A.wipeOk(r)     로그인 응답 하나로 판정 — ok / role / bad / off
A.wipeGate(pw)  window.BNCP_API.login → A.wipeOk     (index.html이 부른다)
```
★비밀번호를 화면 코드에 두지 않는다. 로그인과 **같은 길**로 서버
(`Code.gs`의 `login_()`, 스크립트 속성 `PW_ADMIN`)에 대조한다.
★`role`이 `admin`일 때만 통과. `staff`·`surv`는 거절.
★**통신이 안 되면 초기화 자체를 막는다.** 대조할 방법이 없는데 통과시키면
  잠금이 없는 것과 같다.
★판정과 통신을 갈라 둔 것은 **검사가 판정을 직접 돌릴 수 있게** 하기 위해서다.
★**서버는 손대지 않았다.** `login_()`이 이미 `PW_ADMIN`을 본다 — 재배포 불필요.

검사 [78]. 옛 코드로 되돌려 실제로 실패하는 것을 확인했다.

### ★아직 남은 것 (사용자 확인)
- 비밀번호를 `prompt()`로 받는다. **화면에 글자가 그대로 보인다.**
  가려진 입력창이 필요하면 작은 모달로 바꾼다 — 아직 안 물었다.
- **화면에서 확인 안 됨** : 관리자 비밀번호로 통과하는지 · 스탭 비밀번호로
  거절되는지 · 지운 뒤에 명부와 설계수량이 남아 있는지.

### 0-Z-2-B. ★**미결** — 테스트 자료 일괄 삭제 (사용자 지시로 보류)

★**결론이 안 났다. 다음에 다시 꺼낼 것.** 코드는 하나도 안 건드렸다.

무엇을 지우려는 것인가 — 사용자 말 그대로다.
「업체에서 입력한 자료, 임의로 만든 직영기록, 작업기록, 협력업체명 담당자 등」
「설계는 변하는게 아니라 지울 필요가 없어」
**시범 테스트로 넣은 가짜 자료를 정식 전환 전에 한 번에 치우는 일**이다.
★사용자 확인 : **「초기화를 쓸일은 없지 테스트가 끝나면」** — 즉 **일회성**이다.
그래서 [초기화] 버튼의 범위는 **v2.21.1 그대로 두었다**(협력업체 입력 5종).

```
지울 것   work 작업기록 · crew 인원장비 · insp 검측 · surv 측량 · mreq 자재신청
          direct 직영기록 · vend 협력업체 명부·담당자
남길 것   plan 설계수량 · 마스터 · 별칭
```

★사용자가 **「각각 지우는건 안돼, 한번에 일괄삭제」**라고 못 박았다.
확인대기 목록의 줄별 [삭제]로 하는 것은 답이 아니다.

내놓은 방법 — **브라우저 콘솔 붙여넣기** (배포도 새 단추도 필요 없다)
```js
['work','crew','insp','surv','mreq','direct'].forEach(k => window.APP.S[k] = []);
window.APP.S.vend = [];
window.APP.save();
location.reload();
```
사용자 반응 : **「쉬운 방법 없어」** — 콘솔을 여는 것 자체를 번거롭게 본다.
★**여기서 미결로 남았다.**

★**주소를 바꾸면 되지 않느냐**는 물음이 있었다 → **아니다.**
`vendor.html?v=업체키`의 키를 바꾸면 옛 링크가 안 열릴 뿐, 저장소의 옛 자료는
**그대로 남는다.** 안 보이게 되는 것과 지워지는 것은 다르다. 이 점은 설명했다.

다음에 꺼낼 때 고를 것
- 가 : 콘솔 붙여넣기 (지금 방법 · 새 코드 0)
- 나 : 준비 탭에 **[테스트 자료 일괄 삭제]** 단추를 하나 만든다.
  범위를 체크박스로 고르게 하면 「내가 범위를 정한다」는 요구도 같이 풀린다.
  ★단추를 만들면 **배포가 따르고**, 정식 운영에서 잘못 눌릴 자리가 하나 는다.
  관리자 비밀번호를 [초기화]와 같은 길로 걸어 두면 그 위험은 막힌다.
- ★어느 쪽이든 **기기마다 한 번씩** 돌려야 하고, **구글 시트 원본은 따로**
  `data` 시트 2행부터 지워야 한다(0-Z-5).

### 0-Z-3. ~~구글 시트를 종류별로 가른다~~ → **v2.22.0에서 끝났다**

사용자 물음 : 「시트를 측량·작업현황·검측·자재 등으로 세분화해서 쌓으면
같은 것만 조회하니 속도 늦어지는 걸 늦출 수 있지 않을까?」 → **맞다. 그렇게 했다.**

★현재 행수 **116행**(사용자 확인). 느려질 걱정은 아직 멀다. **그래서 지금 한 것이다** —
옮길 것이 116줄뿐이라 잘못돼도 손해가 없다. 수만 줄 쌓인 뒤에 옮기는 것과는 위험이 다르다.

### 갈라 놓은 시트
```
work  실적   crew  인원·장비   insp  검측   surv  측량
mat   자재   direct 직영       plan  설계수량      etc  모르는 종류
```
★**자재는 서버에서 `mat`이다.** 화면 상자 이름 `mreq`와 다르다 — 헷갈리지 말 것.
★모르는 종류가 와도 `etc`로 받는다. **버리지 않는다.**

### 지켜야 할 것 세 가지
1. ★**`type` 없는 조회가 여러 시트를 합쳐서 준다.** 화면의 증분 수신이
   `type` 없이 부른다 — 이 길이 막히면 **아무것도 안 내려간다.**
2. ★★**캐시 열쇠가 시트마다 따로다**(`idmap:시트이름`). 하나로 쓰면 시트끼리
   행번호가 섞여 **다른 종류의 엉뚱한 줄을 덮어쓴다.**
   ※옛 v4에서는 같은 id가 종류를 넘어 **한 줄로 뭉개졌다** — 검사로 확인했다.
3. ★**옛 `data` 시트를 이관 전까지 같이 읽는다.** 배포와 이관 사이에 자료가
   안 보이는 틈을 만들지 않기 위해서다. 옛 시트는 종류가 섞여 있으므로
   `type` 조회 때 한 번 더 거른다.

### 이관 — editor에서 손으로
```
splitCheck()   무엇이 몇 줄인지 보기만 한다. 아무것도 안 바꾼다.
splitRun()     옮긴다. 끝나면 옛 시트를 data_구판_날짜시각으로 **이름만** 바꾼다.
               ★지우지 않는다. 두 번 돌려도 안 겹친다(있는 id는 건너뛴다).
splitUndo("data_구판_…")   되돌린다.
               ★되돌린 뒤엔 **옛 코드(v4)로 되돌려 배포**해야 앞뒤가 맞는다.
```
★웹앱 주소로는 못 부른다 — 일부러 그렇게 뒀다.
★돌리기 전에 스프레드시트를 **통째로 복제**해 둘 것.

### ★검사 `tools/smoke_gs.js` 신설 — **검사가 이제 네 벌이다**
Apps Script를 흉내 낸 자리에서 서버를 **실제로 돌려 본다.** 글자 대조가 아니다.
저장 갈래·upsert·시트별 캐시·합친 조회·증분·옛 시트 병행 읽기·이관·
두 번 돌리기·되돌리기·로그인. ★옛 v4로 되돌려 7건이 실패하는 것을 확인했다.

### ★`tools/AppsScript_Code.gs`가 낡아 있었다
배포본은 v4인데 저장소 사본은 **3단계에 멈춰** 있었다. 다음 사람이 그 파일을
올리면 측량팀 로그인과 증분 수신이 통째로 사라진다.
→ 이제 `Code.gs`와 **같아야 통과**하는 검사를 걸었다(G8). 고칠 때 두 파일을 같이 고칠 것.

### ★아직 화면에서 확인 안 된 것 (사용자 몫)
- 배포 → `splitCheck()` → `splitRun()` 순서로 돌려 보기
- 돌린 뒤 [전체 다시 받기]를 눌러 **116건이 그대로 보이는지**
- 협력업체 화면에서 새로 하나 올려 `mat` 시트에 붙는지

### 0-Z-4. 용량 80% 경고 + 파일 자동 추가 — 사용자 제안, **나중** (3순위)

사용자 물음 : 「연도별로 나누지 말고 용량이 80% 찰 경우 경고하고
파일 추가하는 거 가능하겠어?」 → **가능하고, 연도별보다 낫다.**

★**연도별로 가르지 않기로 했다.** 달력(1월 1일)이 아니라 **실제 차는 속도**를
따라야 어떤 해는 조용하고 어떤 해는 바쁜 것을 알아서 맞춘다. 사람이 해마다
새 파일을 만들어 주지 않아도 된다.

만들 것 (실제로 느려지는 게 체감될 때 착수)
- 저장할 때마다 `getLastRow()`로 행 수를 본다(이미 매번 부르고 있다).
- 80%에서 관리자에게 경고. 한계 근처에서 **새 스프레드시트를 만들고
  그때부터 거기 쓴다.**
- ★「몇 번째 파일에 언제부터 언제까지」를 적은 **작은 안내표**가 있어야
  조회 때 여러 파일을 오가며 합칠 수 있다. 이것 없이는 옛 파일이 안 보인다.
- ★이 「여러 파일 조회 합치기」는 0-Z-3을 해 두면 자연히 확장된다.
  **0-Z-3을 먼저 할 것.**

### 0-Z-5. 정식 전환 시 초기화 절차 — **문서만, 코드 아님**

사용자 물음 : 「정식으로 사용하면 데이터를 어떻게 초기화해야 되지?
구글시트 자료를 삭제하면 되는 거야?」 → **시트만 지우면 안 된다.**

자료가 **두 군데**에 있다.

| 어디 | 무엇 | 어떻게 |
|---|---|---|
| 구글 시트 | 원본 전체 | `data` 시트 **2행부터** 삭제 (1행 머리글은 남긴다) |
| 각 기기 브라우저 | 그 기기의 사본 | 화면 오른쪽 위 **[초기화]** (관리자 비밀번호) |

★**순서를 지켜야 한다.**
1. 먼저 **모든 기기**에서 [초기화] — 관리자 PC · 스탭 PC · 업체 휴대폰 전부
2. 그 다음 구글 시트를 비운다
3. 그 사이에는 아무도 화면을 쓰지 않는다

시트만 비우면 기기마다 사본이 남아 화면에 계속 보이고, 그 기기에서 뭔가
누르면 **옛 자료가 시트로 다시 올라간다.**

★**v2.21.1부터 [초기화]는 협력업체 입력 5종만 지운다.** 명부·마스터·설계수량은
남으므로 정식 전환 때 다시 올릴 필요가 없다. 그래도 시트를 비우기 전에는
설계수량 파일 정도는 손에 쥐고 있는 편이 안전하다.
★[초기화]에는 **관리자 비밀번호**가 걸려 있다. 각 기기에서 초기화를 돌리려면
관리자가 직접 하거나 비밀번호를 알려 주어야 한다 — 현장 기기가 많으면 시간이 든다.

### 0-Z-6. 저장 문제는 **이미 해결돼 있다** — 다시 건드리지 말 것

사용자 물음 : 「저장 문제는 어떻게 되었지?」 → **v2.18.5에서 끝났다.**

`A.save()`는 저장이 터지면 그 자리에서 90일 넘은 기록을 덜어내고 **한 번 더
시도**한다. 덜어냈으면 무엇을 언제 덜어냈는지 알린다(`cap_trimmed`).
그래도 안 되면 경고창을 띄운다. **조용히 실패하지 않는다.**
★아직 처리가 안 끝난 건은 오래됐어도 안 덜어낸다 — 밀린 일이 사라지면 안 된다.
★덜어내도 자료가 없어지는 게 아니다. 원본은 시트다. 화면은 최근 90일치만
들고 있는 것이고, 필요하면 [전체 다시 받기]로 다시 받는다.

### 0-Z-7. 수치 — 하루 200건 기준 (사용자 질문에 답한 값)

```
용량 한도 : 12열 × 83만 행 ≈ 1000만 셀 → 하루 200건이면 약 11년
속도 저하 : 5만 행에서 체감 시작 → 약 8개월
            10만 행 → 약 1년 4개월
```
★**용량보다 속도가 훨씬 먼저 온다.** 1년 안팎부터 느려지는 것을 체감할
가능성이 높다. 그래서 0-Z-3(종류별 가르기)이 0-Z-4보다 앞이다.

### 0-Z-8. 아직 **화면에서 확인 안 된** 것들

코드와 검사는 통과했으나 **눈으로 본 적이 없다.** 사용자 몫이다(2-C).
- v2.20.0 결재 흐름 — 스탭이 [지급요청]까지 넘긴 뒤 **관리자 화면
  「내 차례」에 뜨는지**
- v2.20.1 양쪽 확인 — 지급까지 올린 뒤 **현황판에 「결재 대기」가 뜨는지**,
  업체 링크에서 **[Received] 단추가 보이는지**
- v2.21.0 명부 — 업체를 만든 뒤 **2단계 드롭다운에 그 업체가 뜨는지**,
  담당자 [수정]이 폼에 값을 채우는지
- 측량팀 로그인 — ★**`PW_SURVEY` 등록 + Apps Script 재배포를 해야 된다.**
  안 했으면 측량팀만 못 들어온다(나머지는 정상).

### 0-Z-9. 운영에서 겪은 것 — 반복하지 말 것

★**깃허브에 올릴 때 `assets/js` 안까지 들어가서 올려야 한다.**
저장소 루트에서 `index.html`만 갈아 끼우고 `assets` 폴더를 그대로 두면
**겉표지만 새것이고 속은 옛것**이라 화면 버전이 안 바뀐다.
실제로 v2.20.1을 올릴 때 이것 때문에 화면에 `V2.18.1`이 계속 떴다.
브라우저 캐시로 오해하고 한참 헤맸다 — **캐시가 아니라 파일이 안 올라간
것이었다.** GitHub 웹 업로드는 폴더째 드래그가 막히는 브라우저가 있어,
`assets` → `js` 로 **들어간 뒤** 그 화면에서 [Add file]을 눌러야 한다.

★`README.md`가 **v2.3.0에 멈춰 있다.** 코드와 무관한 옛 문서다. 언젠가 정리할 것.

---

## 0-A. ~~설계수량 67개가 화면에 안 뜨는 버그~~ → **v2.19.4에서 고쳤다**

진단은 사실이었다. 실제 파일로 재현해 확인했다(104/5 · 33/67 그대로).

- 원인 : `boqHere()`가 `boqLoc`을 `pkLoc('w')`와 대조했다.
  `pk`는 화면이 처음 열릴 때 한 번 정해지고 상단 필터를 안 따라간다.
- 고침 : `boqHere()`는 화면 위치를 보지 않는다. 확인필요가 있으면 뜬다.
  대신 **적용 위치를 카드 부제에 적는다**(`Phase 3-2 · 67건`).
  저장은 어차피 `boqLoc`으로 들어가므로 엉뚱한 곳에 들어갈 위험은 없다.
- ★종전 검사 [31-3]이 오히려 결함을 통과 조건으로 못 박고 있었다. 뒤집었다.
- 검사 [60] 신설 — 글자 대조가 아니라 **실제 파일로 실제로 그려 본다.**
  옛 파일로 되돌려 5건이 실패하는 것까지 확인했다.

★`pkLoc('w')`를 쓰는 나머지 자리는 전부 훑었다. **표시와 저장처가 같은
변수를 보고 있어 어긋나지 않는다** — 고칠 것 없다.
(1287·1294 장비지급 / 2149·2150 시설물 개소 / 2290 자재 공구칸 /
 2555·2569 문자 / 3156 설계외 자재 / 3319 지급대장)

---

## 0-B. ~~자동매칭이 왜 33%뿐인가~~ → **v2.19.6에서 끝났다 (33 → 85)**

★**뿌리는 매칭이 아니라 파서였다.** v2.19.5에서 「P3-2는 전부 부대공 한 대분류로
묶여 있다」고 봤는데 **틀린 진단이었다.** 파일에는 토공·포장공·우수공·오수공·
상수공이 제대로 들어 있었고, 파서가 그것을 놓치고 있었을 뿐이다.

```
i=26  "6. 부대공"   목차[6]=공동구   이름이 다른데도  → ★대분류로 올림
i=30  "2. 포장공"   목차[2]=포장공   2 > 6 실패      →  아래로 강등
```

대분류 판정이 「번호가 앞 항목+1이 아니면 올린다」였다. 번호가 튀는 것만 보고
이름을 안 봤다. `6. 부대공`은 앞이 `4. 잔토처리`라 5를 건너뛴 것뿐인데 대분류로
올라갔고, `gn=6`이 되어 뒤따르는 대분류가 전부 밀렸다.

→ **번호와 이름이 목차와 둘 다 맞아야 대분류다.** `L.no > gn`은 남긴다 —
`1. 토공`은 대분류로도, 우수공·오수공 아래에도 나온다.

### ★부대공은 공종마다 하나씩 있는 것이다 (사용자 확인)

```
토공의 부대공   — 작업로 및 가도보수, 규준틀
우수공의 부대공 — 관로표식테이프, 물푸기, 수밀검사
상수공의 부대공 — 관로표식테이프, 수압시험
```

마스터도 같은 모양이다(`T-08 토공›부대공›규준틀`, `WW-36 상수공›부대공›수압시험`).
★**블럭공사(부대토목)와 헷갈리지 말 것.** 부지/부대는 `site`로 위에서 갈리고
그 밑에 각각 토공·우수·오수·상수가 있다. 후보 풀은 `site`로 잠겨 있어 섞이지
않는다 — 검사 [62]가 유출 0건을 확인한다.

### ★near가 남의 자리를 뺏는 것을 막았다

내역서가 마스터보다 잘게 쪼개져 있을 때 생긴다.

```
내역서 : 삼각표지판 › 터파기 · 되메우기 · 기초설치 · 표지판설치
마스터 : 삼각표지판 — 터파기 · 되메우기 · 기초설치      ← 표지판설치가 없다
→ near가 P-13(기초설치)을 집는데, 바로 위 기초설치 줄이 이미 차지했다
→ P-13 = 107 + 107 = 214.  ★수량이 두 배
```

near는 짐작이고 나머지(exact·spec·name·bag)는 구조다. **겹치면 짐작이 물러난다**
(`readBoqRows`). 물린 줄은 후보를 달고 확인필요로 남는다.

### 결과

```
P3-1  104/109  (그대로 — 손대지 않았다)
P3-2   33/100 → 85/100 · 확인필요 15 · 후보없음 62 → 2
P-13/P-16/P-19 = 107/140/22   (두 배 계상 없음)
```

남는 15건은 사람이 골라야 하는 것들이다.

```
기층     → P-01 부대도로 T=10cm  /  P-28 주도로 T=20cm
표층 #78 → P-07 주도로 T=4cm    /  P-30 부대도로 T=5cm
표지판설치 · 전기맨홀 · 환경관리비 · 수방대책비 — 마스터에 없다
```

v2.19.5의 규칙 3개(대분류 해제 · near 제외 · 느슨한 별칭)는 **남긴다.** 이번
파일에서는 파서가 고쳐져 거의 쓰이지 않지만, 대분류 이름이 목차와 다른 다른
내역서에서 그물 노릇을 한다. 느슨한 별칭은 한 번 고르면 다른 페이즈에도 붙는다.

★**0-B-1(정확 별칭 갈림)은 사라졌다.** 대분류가 제자리를 찾자 `부대공 › 토공 ›
터파기`가 `우수공 › 토공 › 터파기` / `오수공 › 토공 › 터파기`로 갈라져 글자가
같은 두 줄이 없어졌다. 느슨한 별칭 쪽 `'*'` 차단은 그대로 둔다.

---

## 0-B-2. ~~고쳤는데 화면은 그대로 67건~~ → **v2.19.7에서 고쳤다**

★**둘 다 내가 만든 구멍이다.**

1. v2.19.4에서 확인필요 목록을 「저장돼 있으면 무조건 보인다」로 바꿨다.
   그런데 그 목록은 **올릴 당시 계산해 저장해 둔 결과**다. 규칙을 아무리
   고쳐도 저장된 옛 결과가 그대로 뜬다. **지울 길이 없었다.**
   → `[목록 지우기]` 단추(`#bqDrop`). 되돌릴 수 없으므로 반드시 묻는다.
2. 두 html의 script·css에 버전이 안 붙어 있었다. 새 파일을 올려도 브라우저가
   옛 JS를 쓴다. → 전부 `?v=2.19.7`.

★**버전을 올릴 때 고칠 곳이 세 군데다** — `version.js` · `index.html`의 `?v=` ·
`vendor.html`의 `?v=`. 검사 [63]이 어긋나면 잡는다.

---

## 0-E. 올려주신 마스터·원단위 파일 확인 결과 (2026-08-21)

| 파일 | 결과 |
|---|---|
| `BNCP_원단위_생산성_매핑표` 235행 | 앱과 **정확히 일치** |
| `BNCP_현장_실배치_자원표` 27행 | 24행 반영. 빠진 3행은 소계·총계라 **정상** |
| `BNCP_갤러리_원단위_계산기` 67행 | 56행만 반영 — **11행 누락** |
| `BNCP_공종마스터_컬럼통일` | 부지토목 **883** (앱 261) · 부대토목 92 (앱 92) |

### ★기록 정정 — 「622개 모자람」은 틀렸다

시설물 4종은 **이미 부지토목에 들어 있다.** `core.js`가 GL·PS·SP·IT 행렬을
`site:'civil'`, `kind:'F'`로 등록한다. 빠진 것이 아니라 **표현이 다르다.**

```
앱   GL 56행×18열  PS 56×3  SP 63×3  IT 56×2      원단위 행렬
     「거푸집 · 유로폼 · M2」 → 수량 = 원단위 × 개소수
파일 갤러리 59 · 빗물펌프장 213 · 오수중계P/S 159 · 이리게이션탱크 191 = 622
     「토공 · 터파기(토사,육상,인력) · 깊이 0~1m · m3」 → 내역서에서 직접
```

앱 261개는 파일 883개에 **한 글자도 안 틀리고 그대로** 있다(코드·공종명·단위
전부 일치, 앱에만 있는 코드 0건). **별칭은 하나도 안 끊어진다.**

### 남은 일 — 사용자 확인 필요

- **0-E-1 시설물을 어떻게 넣을 것인가 (★답을 못 받았다)**
  사용자는 **시설물 수량도 내역서 파일로 올린다**고 확인했다. 그러면 622개를
  `kind:'C'`로 넣어야 하는데, 그냥 넣으면 개소수 산출(`core.js` 592행)과
  **겹쳐 두 배가 된다.**
  - 가 : 내역서 수량만 쓴다 → 개소수 입력을 없앤다. 단순하지만 원단위 행렬과
    생산성 매핑표 235건이 쓸모없어진다.
  - 나 : 수량은 내역서, 생산성은 매핑표를 계속 쓴다. 손이 더 가지만 자료가 산다.
  ★**공정표(투입인원·기간)를 시설물에도 돌리는지** 물을 것. 안 돌리면 가, 돌리면 나.
- **0-E-3 갤러리 누락 11행** — 뒤 7개(그레이팅·슬리브·도어락)는 부속 자재라
  무해하나 앞 4개는 다르다 : `토공 터파기·되메우기·잔토`, `철근 복잡 H32`.
  매핑표에는 있는데 매트릭스 값이 전부 0이라 빠졌다. `원단위_토공` 시트에
  실제 값이 있다(본선 50-04 터파기 32.28 M3/m, 되메우기 25.60 M3/m).
  ★끌어올지 물을 것. 구조물 수량에 직접 들어가는 숫자다.

---

## 0-E-2. ~~부대 → 부지 폴백~~ → **v2.19.8에서 넣었다**

```
단지내 부대토목-토공 › 부대공 › 규준틀        → T-08   [exact@civil]
단지내 부대토목-오수공 › 부대공 › 관로표식테이프  → SS-36  [exact@civil]
```

★**한 방향만.** 부지토목 내역서는 부대토목(A-*) 코드를 절대 안 본다.
★**순서** — 부대토목 안에서 먼저 다 찾고, 못 붙였을 때만 부지토목을 본다.
후보 목록도 부대토목이 앞이다. 손으로 고를 때도 부지토목이 붙는다(`bq_allc`).
`A.boqMatch`를 껍데기 + `bqTry(it, site)`로 나눴다. 별칭은 껍데기에 남는다.

---

## 0-C. ~~형식이 아예 다른 파일 — 지급장비~~ → **v2.19.12에서 읽는다**

`장비현황-1.csv`는 4열 양식이 아니라 **장비 종류가 열 머리로 가로로 펼쳐진**
팀별 일일 투입표다(34종·날짜 칸 없음). 종전 4열 판독기는 장비 열을 못 찾아
**한 줄도 안 읽었다.**

사용자 지시 : 「형식에 상관없이 읽어서 가지고 와야지 좌던 우던간에」

- `A.readEquipFile(rows, loc, date)`이 입구다. 4열이면 4열로, 아니면 가로로.
- 이름 맞추기(`A.eqFindName`)가 핵심이다. **낱말과 숫자를 따로 본다.**
  ```
  파일   Excavator2.9㎥(Crawler)   Dozer18 Ton      Fork Lift (16 Ton )
  마스터 Excavator(crawler) 2.9m3  Dozer(D6R) 18ton  Fork Lift 15ton
  ```
  ★글자와 숫자가 붙어 있으면 떼어 낸다(`dozer18` → `dozer` `18`).
  ★단위 `㎥`의 3을 먼저 지운다 — 안 지우면 규격이 1.2가 아니라 3으로 읽혀
    전부 2.9m3로 몰린다. **실제로 한 번 그렇게 나왔다.**
  → 34종 중 **31종을 읽고 못 알아본 것 0**.
- 마스터에 없는 규격은 **근사로 맞추고 화면에 알린다**(Dozer 26→27ton,
  Fork Lift 16→15ton, Combi Roller 3.7→1ton, Excavator 0.3→0.2m3,
  TRAILER→Lowbed Trailer). 짐작을 숨기지 않는다.
- ★**아직 안 한 것 — 사용자 확인 필요**
  - 파일 안 구역(부대토목현장 · 부대토목 SI · 부지조성현장 · 부지조성 SI)을
    **나누지 않았다.** 구역을 위치(Phase/Town)로 옮길 규칙이 파일에 없다.
    지금은 **화면에서 고른 위치 하나로 전부** 들어간다.
  - 날짜 칸이 없어 **올린 날**로 넣는다.
  - 이 숫자는 원래 「그날 투입한 대수」다. 지급대장(S.issue = 보유)으로 넣었다 —
    사용자가 지급대장 자리에 올린 파일이기 때문이다. 가동으로 봐야 한다면 말할 것.

## 0-H. ~~오늘 투입 인원 0명 · 장비 0대~~ → **v2.19.12에서 고쳤다**

★**v2.18.9의 빠진 짝이었다.** 「실적은 어제 · 투입은 오늘」로 가르면서
**관리자 표만** 오늘로 바꾸고 **협력업체 입력 폼은 어제로 남겨 뒀다.**
업체가 기본값 그대로 인원·장비를 올리면 어제 날짜로 저장돼, 오늘 기준인
관리자 인원·장비 표에 영영 안 나온다.

```
업체 폼   실적 어제 · 인원장비 어제 ← 여기가 안 바뀌어 있었다
관리자 표 실적 어제 · 인원장비 오늘
```

→ 인원·장비 폼을 오늘로. 실적 폼은 어제 그대로다. 검사 [V2-2].

---

## 0-D. ~~현황판 시각 손질~~ → **v2.19.9에서 고쳤다 (갈래 「가」)**

진단은 사실이었다. `siteTable()`의 `<thead>` 8칸은 멀쩡히 들어 있었고,
**머리행이 없는 게 아니라 덮인 것**이 맞았다.

- 겹치는 축이 둘이었다 — `.sb`는 **화면 기준** sticky(불투명), `.tw th`는
  **상자 안 기준**(`top:0`). `.tw`가 자체 스크롤 상자(520px)라 상자 윗부분이
  띠 뒤로 들어가면 머리행도 카드 제목도 같이 묻혔다.
- 사용자가 **「가」**를 골랐다 → **`.sb`에서 sticky를 걷어냈다.** 띠는 본문과
  같이 스크롤돼 올라간다. v2.18.0의 「위로 올릴 때만 내려온다」는 없어졌다.
- 같이 지운 것(3-E) : `body.scr-dn` 규칙 · `tabs.js`의 스크롤 방향 감시 ·
  쓰지 않게 된 `--tabh`.
  ★**되살리려면 겹침부터 풀 것.** 그냥 되돌리면 증상도 그대로 돌아온다.
- `.tw`의 자체 스크롤과 상자 안 머리행은 **손대지 않았다** — 「나」를 골랐다면
  풀어야 했던 자리다. 진행률 표·자재 표에 번지지 않는다.
- ★종전 검사 다섯 줄이 오히려 sticky를 **통과 조건으로 못 박고** 있었다
  ([31-4] 3줄 · [49-1] 2줄). 뒤집었다. 검사 [65] 신설.

### ★화면에서 확인됐다 (사용자 캡처 · v2.19.9)

카드 제목 「현장 현황」과 머리 8칸이 정상으로 보인다. 띠는 본문과 같이
올라가고 아무것도 덮지 않는다. **끝난 일이다.**

## 0-D-2. ~~장비 숫자가 앞뒤가 안 맞던 것~~ → **v2.19.9에서 고쳤다**

띠는 `장비 2대 · ●30 ●7`인데 아래 장비현황 카드는 「대조할 게 없습니다」였다.

★원인 : **`summaryHTML`만 기간을 안 잘랐다.**

| 자리 | 무엇으로 셌나 |
|---|---|
| 띠 · 인원 | `resAgg(A.today(), A.today())` → 오늘 |
| 띠 · 장비 | `A.eqStatus(flt)` → **상단 필터 기간(기본 전체 누계)** |
| 장비현황 카드 | `withRng('eq')` → 오늘 (4-C 규칙) |

오늘 입력이 없으면 카드는 비고 띠에만 누계가 남았다. 「보유 2대인데 가동 30대」도
같은 원인이다 — 보유는 오늘 지급대장, 가동은 누계라 애초에 뺄 수 없는 두 숫자다.

→ 사용자 지시대로 **오늘 기준으로 통일.** `withDay(d, fn)`를 새로 뒀다.
`withRng`는 표마다 걸린 기간 단추를 따르지만 **띠에는 기간 단추가 없다** —
띠는 언제나 오늘이므로 날짜를 직접 박는다. 검사 [66] 신설.

화면에서 `장비 0대 ●0 ●0`과 「대조할 게 없습니다」가 서로 맞는 것을 확인했다.

## 0-D-3. ~~확인필요 카드 자리 · 공구 표 열 너비~~ → **v2.19.10에서 고쳤다**

- **확인필요 카드를 요약 띠·현장 현황 아래로 내렸다.** 50건이 뜨자 카드가 첫
  화면을 통째로 채워 오늘 숫자가 묻혔다(사용자 캡처). 4-B 순서는 문서에만
  있었고 코드는 반대였다 — 이제 같다. 경고·확인대기·재확인요청은 그대로 위다.
- **공구 표 열 너비** — 여덟 칸이 남는 폭을 고루 나눠 값 사이가 비어 보였다.
  일곱 칸은 `width:1%`+`nowrap`으로 내용만큼만 쓰고 남는 폭은 **작업위치**가
  받는다. ★`.tw--site`로 **이 표에만** 건다. 진행률·자재 표는 그대로다.
- 검사 [67]. 순서만 되돌려도, 너비만 되돌려도 실패하는 것을 확인했다.
- ★이것도 화면에서 다시 봐 주셔야 한다 — 이쪽은 렌더 결과를 못 본다(2-C).

## 0-G. ~~직영 작업 기록~~ → **직영현황 (v2.19.11)**

사용자 지시로 셋을 같이 고쳤다.

- **이름** `d_list` : 직영 작업 기록 → **직영현황** (ko/en/bn만. ★`ar`은 협력업체
  전용 부분 사전이라 건드리지 않는다 — 3-C).
- **기본 기간 = 오늘** (`RNG_DEF.dir='today'`). 직영은 인원·장비와 같은 「투입」이다.
  ★종전 `A.directRows`는 **위치만 보고 날짜를 아예 안 봤다** — 전 기간이 다 나왔다.
- **기간 단추**(`rngBtn('dir')`) — 오늘·7일·30일·전체·[직접]. 몸통은
  `withRng('dir', …)`로 감쌌다. 관리자 작업현황 쪽 카드도 같은 `v7()`이라 함께 바뀐다.
- 검사 [68] · smoke_direct [D9]. `A._rng(id, r)` 통로를 열었다(6-B) —
  기간 단추 처리기는 브라우저에만 붙어 이것 없이는 조회를 검사할 길이 없다.

★**[D6]의 오래된 거짓 통과를 같이 바로잡았다.** 「탭7에 검측·측량·자재가 없다」가
관리자로 `A.go(7)`을 불렀는데 관리자는 탭7이 없어 **탭1로 되돌아간다**. 즉 탭7이
아니라 탭1을 보고 있었고, 자료가 비어 현장 현황 카드가 안 그려졌기에 통과했을
뿐이다. 역할을 **스탭**으로 바꿔 진짜 탭7을 보게 했다.

## 0-I. ★다음 작업 — 장비 보유를 **업체 축**으로 (사용자 확정 · 미착수)

### 사용자가 확정한 것

- 「장비는 **업체에 주는 것**이지 부지·부대에 주는 게 아니다」
  → 보유를 **위치로 나누지 않는다.** 업체 기준으로 가른다.
- `장비현황-1.csv` **안의 숫자 = 지급한 대수**가 맞다.
  (제목의 `-1`은 아무 뜻 없는 순번이다 — 이쪽이 한 번 헷갈려 되물었다.)

### 파일을 뜯어본 사실

```
- 부대토목현장          ← 구역 (4개: 부대토목 · 부대토목 SI · 부지조성 · 부지조성 SI)
    토공 | 터파기등            ← 행은 공종/팀 + 작업내용
    오수공팀 | 오수관/맨홀 …
  열은 장비 34종, 값은 지급 대수
```

★**업체명이 파일 어디에도 없다.** 행은 공종·팀이다. 그러니 **올릴 때 업체를
골라야 한다** — 파일에서 뽑을 방법이 없다.
★한 업체가 부지·부대를 다 맡으면 파일 하나가 그 업체의 지급 총량이다.
종류별로 전부 합치면 된다 — 지금 판독기가 하는 것이 정확히 그 합계다.
★날짜 칸도 없다.

### 착수 순서

1. **업체 칸** — 파일 올릴 때 업체를 고르고 `S.issue`의 `by`에 박는다.
   지금은 손입력도(`core.js` `setEqQty`) 파일도 `by: ''`라 늘 비어 있다.
   그래서 장비현황 카드의 「업체별」 줄이 항상 비었던 것이다.
2. ★**쌓지 말고 덮어쓴다.** 지급 대수는 그때그때의 **상태값**이다.
   그런데 v2.19.12 판독기는 읽은 값을 **새 줄로 push**한다 —
   **같은 파일을 두 번 올리면 보유가 두 배가 된다.** 아직 안 고쳤다.
   `A.setEqQty`가 쓰는 「같은 자리를 고친다」 방식을 업체별로 적용할 것.
3. **`A.eqRecon`의 보유를 업체 기준으로.** 지금은 `A.hit(g, f)`로 위치를 본다.
   ★여기가 이 작업에서 유일하게 조심할 자리다. 한 줄이지만 값이 넷으로 퍼진다 —
     장비현황 카드 · 요약 띠의 `장비 N대` · 업체별 보기 · 유휴 계산.
   ★요약 띠의 장비 숫자는 v2.19.9에서 이미 두 번 고친 자리다(0-D-2).
     **검사를 붙여 가며 한 칸씩 옮길 것.**
4. 카드·요약 띠의 보유·유휴가 새 축을 따라가게.
5. 검사 — ①두 번 올려도 안 쌓인다 ②업체가 갈린다 ③위치를 좁혀도 보유가
   안 줄어든다(업체 축이므로) ④가동은 종전대로 위치별이다.

### 이미 끝난 것 — 다시 만들지 말 것

- 가로 표 판독(`A.readEquipWide`)과 이름 맞추기(`A.eqFindName`)는 **완료**다.
  34종 중 31종, 못 알아본 것 0. 근사 5건은 화면에 알린다. 검사 [69].
- ★`A.eqFindName`은 지급대장 판독에도 그대로 쓴다 — 새로 만들지 말 것.

### ★사용자 화면 정리

v2.19.12로 그 파일을 올렸다면 지급대장에 **31줄이 업체 없이** 들어가 있다.
새로 시작하기 전에 장비현황 카드에서 지우거나 [초기화]로 털 것.

## 0-F. ★「확인 필요 50건」은 고장이 아니다 — 저장된 옛 목록

v2.19.9 캡처에서 확인필요가 50건이었다. **지금 파서 결과가 아니다.**

```
화면   부대공 › 기층/보조기층 › 기층   16,800.0 m3   ← v2.19.6 이전 파서 모양
지금   포장공 › 기층/보조기층 › 기층   16,800.0 m3   ok 85/100 · 확인필요 15
```

수량이 `sample/P3-2.csv`와 한 자도 안 틀리는데 대분류만 「부대공」이다.
화면에 `기초설치`와 `표지판설치`가 둘 다 뜬 것도 같은 근거다 — 지금은
기초설치(107/140/22)가 P-13/P-16/P-19로 붙는다.

→ **[목록 지우기] 누르고 내역서를 다시 올리면 된다.** 0-B-2에서 만든 단추가
정확히 이 경우를 위한 것이다. ★확인필요 건수가 이상하면 **먼저 이것부터 의심할 것.**

---

# 1. 이 시스템이 무엇인가

이라크 BNCP 현장의 토목 공사 관리 대시보드다. **화면이 둘이다.**

| 화면 | 누가 | 무엇을 |
|---|---|---|
| `vendor.html` | 협력업체 | **입력만** — 실적·인원장비·검측신청·측량신청·자재신청 |
| `index.html` | 우리 스탭·관리자 | **확인·승인·지급·집계** |

- 서버 없는 **정적 파일**이다. GitHub Pages에 올려 쓴다.
- 자료 원본은 **구글 시트**. Apps Script가 API 노릇을 한다.
- 브라우저는 `localStorage`에 최근 것만 들고 있는 **캐시**다(90일).

## 1-A. 파일 구조
```
index.html          관리자·스탭 화면
vendor.html         협력업체 입력 화면
assets/js/
  core.js           자료·계산 (A.* 전역)
  tabs.js           관리자 화면 그리기·이벤트
  vendor.js         협력업체 화면 (IIFE, window.VENDOR = {render, state})
  api.js            서버 통신 (window.BNCP_API)
  i18n.js           4개 언어 사전
  master.js         공종 마스터    materials*.js 자재 마스터
  equip.js          장비 마스터    spot.js 측점(STA)
tools/
  smoke.js          관리자 화면 검사 (773건)
  smoke_vendor.js   협력업체 화면 검사
  smoke_direct.js   직영 검사
Code.gs             ★Apps Script (구글 시트 쪽에 붙이는 것 — 별도 전달)
```

## 1-B. 배포
1. zip을 풀어 GitHub 저장소에 **전부 덮어쓰기** (Add file → Upload files)
2. Commit → 1~2분 뒤 Pages 반영
3. 주소 : `https://jonguklee3214-star.github.io/bncp-dashboard/`

Apps Script를 바꿨으면 **[배포]→[배포 관리]→연필→「새 버전」→[배포]**.
★**[새 배포]를 누르면 주소가 바뀐다. 절대 누르지 말 것.**

---

# 2. 사용자에 대해 — 일하는 방식

★**이 절을 반드시 읽을 것.** 여기 어긋나면 작업이 통째로 되돌려진다.

## 2-A. 지시받은 것만 한다
- **임의로 추가하지 말 것.** v2.17.x에서 탭 7개를 2개로 통합하고 자재를
  단순화했다가 **전량 폐기**당했다. 지시를 「해석」해서 바꾼 것이 원인이다.
- 애매하면 **묻는다.** 다만 **묻기 전에 코드와 이 문서를 먼저 뒤진다.**
  이미 답이 있는 것을 물으면 지적받는다.
- 「대답만 해」라고 하면 **코드를 건드리지 않는다.**

## 2-B. 짧게 지시한다
큰 그림은 이쪽이 정리해 드려야 한다. 다만 **정리만 하고 착수는 확인 뒤에.**

## 2-C. 화면을 직접 보고 잡아낸다
컨테이너에 브라우저가 없어 이쪽은 렌더 결과를 눈으로 못 본다.
**시각적 판단이 필요한 일은 만들고 나서 확인받는다.**
사용자가 캡처로 잡아낸 버그가 여럿이다(빨간 글씨, 현황판 잘림, 자재 신청 누락).

## 2-D. 보고는 사실대로
- 안 한 것을 했다고 하지 않는다.
- 인수인계서 진단이 실제와 다르면 **그 사실을 말한다.**
  (실제로 v2.16.2에서 「화면이 깨졌다」던 진단이 죽은 코드였던 적이 있다)
- 확신 없이 판단했으면 **밝힌다.**

---

# 3. 반드시 지킬 규칙

## 3-A. 검사 없이 끝내지 않는다
사고가 나면 고치는 것으로 끝내지 말고 **그 사고를 잡는 검사를 반드시 추가한다.**

```bash
node tools/smoke.js && node tools/smoke_vendor.js && node tools/smoke_direct.js
for f in assets/js/*.js; do node --check "$f"; done
```

★검사를 만들었으면 **옛 파일로 되돌려 실제로 실패하는지 확인할 것.**
안 그러면 거짓으로 통과하는 검사가 된다. 실제로 그런 적이 여러 번 있다.

## 3-B. 검사 기준어 함정 — 세 번 당했다
화면에서 문구로 위치를 찾을 때, **그 문구가 다른 곳에도 있으면 엉뚱한 자리가
먼저 잡힌다.**

| 기준어 | 어디에 또 있었나 |
|---|---|
| 「실측 생산성」 | 확인대기 카드 부제 |
| 「장비현황」 | 현황판 문구 |
| 「작업위치」 | 공구 표 칸 머리 |

→ 카드에만 있는 고유 문구를 쓰거나, 화면 전체에서 찾을 것.

## 3-C. i18n 삽입 — 네 번 당했다
**아랍어(`ar`) 사전은 협력업체 화면 전용 부분 사전이다.** 관리자 화면 키가 없다.
자동 삽입을 하면 **아랍어 자리에 벵골어가 들어간다.**

→ **`ko` `en` `bn` 세 곳에만 넣고 `ar`은 건너뛴다.** 넣은 뒤 반드시 확인한다.
```bash
node -e "…; ['ko','en','bn'].forEach(l=>console.log(l, I[l].새키))"
```

## 3-D. 같은 이름 두 번 정의 — 두 번 당했다
| 언제 | 무엇 |
|---|---|
| v2.16.1 | `eqHTML`·`eqBind` 두 번 → 장비 입력폼이 죽음 |
| v2.18.8 | `A.matRows` 두 번 → **자재 신청이 화면에서 통째로 사라짐** |
| v2.22.1 | `todayOrOpen` / `A.todayOrOpen` → ★**이름이 달라 검사에 안 걸렸다.** 한쪽만 고쳐져 끝난 측량·자재가 안 보임 |

smoke [31]·[54]가 검사한다. **새 함수를 만들 때 이름을 grep으로 먼저 확인할 것.**

## 3-E. 죽은 코드를 남기지 않는다
쓰지 않게 된 함수·CSS는 지운다. 남겨 두면 다음 사람이 되살릴 자리로 착각한다.
되살릴 가능성이 있으면 **플래그로 명시**한다 (`SCHED_ON`, `MAT_FLOW_ON`).

## 3-F. 값과 표시를 가른다
자재·공종의 **값(value)은 한국어 원문 그대로 둔다.** 보이는 글자만 번역한다.
값을 바꾸면 `matId`·`itemsOf` 조회가 어긋나 목록이 통째로 빈다.

---

# 4. 화면 구조

## 4-A. 탭
```
1 작업현황   2 검측   3 측량   4 자재현황   5 알림·전파   7 직영
(6 공정표는 SCHED_ON=false 로 감춰 둠)
```
★**관리자에게는 직영 탭(7)이 안 보인다.** 작업현황 안에 들어가 있다.
스탭은 탭7에서 입력한다 — 「합치는 것이 아니라 추가하는 것」이라는 지시다.
★**관리자로 `A.go(7)`을 부르면 탭1로 되돌아간다**(TABS_ON). 검사에서 탭7을
보려면 반드시 **스탭**이어야 한다 — [D6]이 이것 때문에 오래 거짓 통과했다.
★직영 카드 이름은 **직영현황**이고 기간은 **오늘**이 기본이다(v2.19.11).
★**측량팀에게는 탭3(측량) 하나만 보인다**(v2.20.0). 들어와서 할 일이
「완료 / 완료 못한 사유」 입력 하나이므로 나머지를 보여 주면 실수할 자리만 는다.
★**문지기는 `A.isIn()`이다. `A.isStaff()`가 아니다.** 측량팀은 스탭이 아니라서
`isStaff()`로 막으면 비밀번호가 맞아도 영영 못 들어온다. 검사 [28]이 지킨다.

## 4-B. 작업현황 순서 — smoke [41]이 고정
```
경고(alert) · 확인대기 · 재확인요청
→ 요약 띠 → 현장 현황(공구 표) → 내역서 확인필요
→ ① 인원 → ② 장비 → ③ 직영(관리자만) → ④ 작업량 → ④-2 작업위치
→ ⑤ 진행률 → ⑥ 생산성 → ⑦ 준비
```
★내역서 확인필요의 자리는 **v2.19.10에서 요약 띠 아래로 내렸다** — 검사 [67].
종전 문서는 경고·확인대기까지 아래에 있는 것처럼 적혀 있었으나 그건 틀렸다.
그 셋은 예나 지금이나 맨 위다.

## 4-C. ★기간 기준 — 표마다 다르다
```js
var RNG_DEF = { out:'today', loc:'today', ppl:'today', eq:'today',
                insp:'today', surv:'today', mat:'today', dir:'today' };
```
| 표 | 기준 | 왜 |
|---|---|---|
| 작업량·작업위치 | **오늘** | ★v2.19.13 — 「반영된 날짜 기준이니 오늘로 통일」 |
| 인원·장비·직영 | **오늘** | 투입은 아침에 정해진다 |
| 검측·측량·자재 | **오늘** | |
| 진행률·생산성 | 누계 | 설계 대비 누적이라 자르면 뜻이 없다 (★그대로 둔다) |

★**v2.18.9의 「작업량·작업위치=어제」는 v2.19.13에서 뒤집혔다.** 어제로 두면
업체가 **오늘 올린 실적**이 걸러져 표에서 사라진다. 기준은 일한 날이 아니라
반영된 날이다.
★**협력업체 입력 폼 기본 날짜도 같이 오늘이다**(넷 다). 표와 폼 중 한쪽만
바꾸면 올린 것이 화면에 영영 안 나온다 — 0-H가 그 사고였다.

★**「작업위치」가 두 뜻이다. 헷갈리지 말 것.**
- 작업량 표 밑 = **어제** 어디서 얼마나 했나 (실적, `spotTable`)
- 현장 현황 칸 = **오늘** 어디에 나와 있나 (투입, `A.siteRows`)

## 4-D. ★오늘 + 미처리
확인대기·검측·측량은 **처리한 것은 오늘 것만, 안 한 것은 날짜 무관하게** 남는다.
안 그러면 어제 밀린 검측이 사라져 그 일 자체가 없어진다.
```js
A.todayOrOpen(rec, isOpen)   // core.js
```
★**불합격·지연은 미처리다.** 재검측이 남아 있다. **합격만 끝난 것.**

## 4-E. 공구가 공통 축
작업현황·공구표·검측·측량·자재가 **모두 공구를 첫 칸**으로 시작한다.
화면마다 축이 달라 머릿속에서 다시 맞춰야 했던 것을 없앴다.

---

# 5. 자료 구조

## 5-A. 저장소 (`localStorage` 키 `bncp.dash.v2`)
```
S.work    실적          S.crew   인원·장비
S.insp    검측          S.surv   측량
S.mreq    자재          S.direct 직영
S.plan    설계수량 {위치키:{공종코드:수량}}   ← 배열이 아니다
S.issue   장비 지급     S.stock  자재 재고(직접입력)
S.vend    협력업체 명부 S.boq    내역서 확인필요 목록
S.tab     마지막 탭     S.rxLast 서버 마지막 수신시각(증분 기준)
S.trimMsg 덜어낸 안내
```

## 5-B. 상태값
```
실적·인원장비 : sub(제출) → ok(확인) / rej(반려)
검측          : apply → ready → sub → pass / fail / delay
측량·자재     : ★결재 흐름(r.fst) — 5-D 참조
```
★측량의 `done`과 자재의 `st`는 **남겨 두었다.** 옛 기록·집계·서버가 그대로
읽는다. `A.flowGo`가 단계를 옮길 때 이 둘을 같이 맞춰 준다.

★수신 병합 시 **상태에 순서를 매겨 앞선 쪽을 남긴다.**
```js
{ sub:0, apply:0, rej:0.5, ready:1, ok:2, pass:2, iss:2 }        // 옛 상태
{ req:0, back:0.5, rej:0.5, chk:1, ord:2,
  sdone:3, sfail:3, delay:3, iss:3, fin:4, none:4 }              // 결재 단계
```
전송이 늦어도 서버의 옛 값이 내 확인을 되돌리지 못한다.
★**되돌아간 것(back·rej)은 앞선 판정이다** — 방금 누군가 반려한 것이므로
서버의 낮은 단계가 덮으면 안 된다. 그래서 req(0)보다 위(0.5)에 둔다.
v2.18.8에서 rej를 sub 위에 둔 것과 같은 이유다.

## 5-C2. ★협력업체 명부 — 두 단계 (v2.21.0 사용자 지시)

```
1단계  업체코드 + 업체명       → A.vendCreate()    ★담당자를 안 받는다
2단계  업체를 **고르고** 담당자 → A.vendStaffAdd()
```
★종전에는 한 폼에서 업체코드·업체명·담당자를 **매번 다시** 적었다.
코드가 같으면 합쳐지긴 했으나, 업체명을 그때그때 타이핑하다 **한 글자만
달라도(KEW / K.E.W) 같은 업체가 둘로 갈라진다.** 이제 업체는 한 번 만들고
담당자는 드롭다운에서 골라 붙인다 — 다시 적을 일이 없으니 갈라질 자리가 없다.

★같은 코드를 다시 만들면 **이름만 고치고 링크(`key`)는 유지한다.**
링크가 바뀌면 협력업체가 쓰던 주소가 죽는다.

### ★전화번호는 담당자마다다
종전에는 업체에 하나뿐이라(`v.tel`) 독촉이 늘 대표번호로만 갔다 — 누가
처리해야 하는지 모른 채 업체 안에서 돌기만 한다.

★**구조를 갈아엎지 않았다.** `staff`는 종전대로 문자열 배열이고 칸만 하나 더 쓴다.
```
'Ahmed'                    옛것 — 이름만
'토공|Ahmed'               옛것 — 공종|이름
'토공|Ahmed|964770000001'  새것 — 공종|이름|전화
'|Ali|964770000002'        ★공종이 없어도 앞 칸을 비워 자리를 지킨다
```
마지막 줄이 중요하다 — 안 비우면 **이름이 공종 자리로 밀려 들어간다.**
`A.vendStaffMake(grp, name, tel)`가 이 규칙을 지킨다.

```js
A.vendCreate(code, name)                  1단계
A.vendStaffAdd(code, grp, name, tel)      2단계 (같은 공종·이름이면 고친다)
A.vendStaffSet(code, oldRaw, grp, nm, tel) 줄 통째로 교체
A.vendStaffDel(code, raw) · A.vendDel(code) · A.vendReset()
A.vendTel(v, grp, name)   이름 → 공종 → 기본 → 업체번호 순
A.vendAdd(...)            ★남겨 뒀다 — CSV 일괄 업로드가 쓴다
```
★`A.vendTel`은 **빈손으로 안 돌려보낸다.** 못 찾으면 있는 번호라도 준다.

### ★화면에서 지키는 것
- **업체가 없으면 담당자 폼을 아예 그리지 않는다.** 빈 드롭다운을 띄우면
  눌러 보고 나서야 안 된다는 걸 안다.
- **번호 없는 담당자는 빨갛게** 표시한다. 독촉이 못 가는 사람이라,
  모르고 지나가면 그 자리에서 결재가 멈춘다.
- **지우기는 셋 다 확인을 받는다**(업체·담당자·초기화). 종전에는 × 를
  스치기만 해도 담당자가 사라졌다 — 되돌릴 길이 없다.
- **초기화는 명부만 비운다.** 실적 자료는 건드리지 않는다.

## 5-D. ★결재 흐름 — 자재·측량 공용 (v2.20.0 사용자 지시)

**자재와 측량은 같은 모양이다.** 그래서 엔진을 **한 벌만** 둔다 (`A.FLOW`).
이름만 다르다(지급요청/측량요청 · 지급/측량지시).

```
자재  신청(업체) → 검토(스탭) → 확인(관리자) → 지급 → ★양쪽 확인 → 종료
      req          chk          ord           iss    (okS+okV)   fin
측량  신청(업체) → 확인(스탭) → 측량지시(관리자) → 측량팀 → 최종완료
      req          chk          ord              sdone/sfail  fin
```

반려·곁길
```
back   업체 재검토   (스탭이 반려)      → 업체가 되올리면 req
rej    스탭 재검토   (관리자가 반려)    → 업체까지 안 간다
none   측량 불필요   (스탭 선에서 종결) → 관리자까지 안 올린다
delay  지연          (스탭 조치 중)     → 끝난 것이 아니다
```

### ★복잡해지는 것은 단계 수가 아니다. 아래 셋을 규칙으로 막는다.
1. **반려는 항상 한 칸만 뒤로.** 어디로 보낼지 고르게 하지 않는다.
   가는 곳은 `A.FLOW` 표가 정한다 — 부르는 쪽은 `ok`/`no`/`alt`만 말한다.
2. **버튼은 둘뿐.** 단계마다 이름만 바뀐다(승인/반려 · 완료/미완료).
   셋째(`alt`)는 측량의 「측량 불필요」 하나뿐이다.
   ★셋째 버튼을 더 만들지 말 것. 그 순간 복잡해진다.
3. **사람은 「내 차례」만 본다.** `mineCard()`가 맨 위에 온다.
   단계가 다섯이어도 각자에겐 한 줄이다.
   ★**관리자를 스탭 자리에 끼워 넣지 않았다.** 끼워 넣으면 관리자 목록이
     스탭 것으로 가득 차 정작 관리자가 볼 것이 묻힌다. 전체는 아래 표에서 본다.

### ★마지막 단계는 「한 사람」이 아니라 「양쪽」이다 (v2.20.1)
`A.FLOW.mat.iss`에 `dual:1`이 붙어 있다.
**스탭 최종확인(`okS`)과 업체 수령확인(`okV`)이 둘 다 있어야** `fin`으로 간다.
한쪽만 누르면 `iss`에 그대로 남고, 남아 있는 동안 경고가 뜬다.
사용자 말 그대로다 — 「서로 확인되는 거잖아. 그러면 최종 종료.」

- ★누가 눌렀는지는 **`o.as`(역할)** 로 받는다. **이름(`o.by`)으로는 못 가린다** —
  업체 담당자 이름이 비어 있을 수도, 스탭 이름과 같을 수도 있다.
- ★**뒤로 가면 `okS`·`okV`를 지운다.** 안 지우면 되돌렸다가 다시 지급됐을 때
  옛 확인이 남아 한쪽만 눌러도 종료된다.
- ★**같은 단계면 「켜진 쪽」이 이긴다**(수신 병합). 스탭과 업체가 각자 다른
  기기에서 누르므로, 서로의 확인이 상대를 지우면 영영 종료되지 않는다.
- ★수령확인 줄은 **날짜와 무관하게** 업체 화면에 뜬다. 어제 받은 자재를
  오늘 확인하는 일이 흔하다.

### ★현황판 「결재 대기」 칸 (v2.20.1 — `sbAppr()`)
「현황판만 봐도 승인이 안 되고 있는 것을 경고할 수 있게」라는 지시다.
`A.flowWarn(f)` → `{wait, late, recv, byOwn}`.
- 멈춘 것(60분 초과)이 있으면 **빨강**, 아직이면 주황.
- ★**0이면 칸 자체를 만들지 않는다.** 늘 「0」이 떠 있으면 눈이 그 자리를
  건너뛰게 되고, 정작 숫자가 붙었을 때도 안 보인다.
- 「수령확인 안 됨」을 따로 센다 — 지급은 끝났는데 서로 확인이 안 걸린
  상태라 다른 대기와 성격이 다르다.

### API
```js
A.FLOW                        단계 표 — 여기만 고치면 흐름이 바뀐다
A.fst(kind, r)                지금 단계 (옛 기록도 읽는다 · 마이그레이션 없음)
A.flowGo(kind, r, dir, o)     한 칸 옮긴다  o = {by, why, qty}
A.flowOwn / flowMine / flowEnd
A.flowLate(kind, r, now)      0 아직 / 1 1차 / 2 2차
A.flowMineList(f, role, now)  화면 독촉
A.flowDue(f, now)             문자 독촉 (역할별 · ★업체는 이름별)
A.flowWarn(f, now)            현황판 집계 {wait, late, recv, byOwn}
A.flowOwns(kind, r)           아직 안 누른 사람들 (양쪽 확인이면 둘)
A.flowMineVendor(kind, r)     협력업체 화면 전용
```
★**독촉에서 업체는 「이름」으로 잡는다.** 스탭·관리자·측량팀은 자리에 걸린
일이라 역할 이름을 쓰지만, 업체는 **보낼 곳이 있어야** 왓츠앱 링크가 걸린다.

★**뒷문을 막아 두었다.** 흐름을 건너뛰고 곧바로 지급되던 옛 단추
(`data-mtiss`·`data-mtno`)와 측량 [완료] 토글(`data-sdone`)을 **삭제했다.**
되살리면 스탭 검토도 관리자 확인도 건너뛴다. 검사 [57]·[77]이 없는 것을 지킨다.

★**옛 기록을 버리지 않는다.** `fst`가 없으면 종전 필드에서 유추한다
(자재 `st:'iss'` → `fin`, 측량 `done:true` → `fin`).

## 5-E. ★자재 신청 수신 갈래 — 종전에는 **아예 없었다** (v2.20.0에서 발견)
`unpack()`의 box 표에는 `mreq`가 있는데 갈래가 없어, 협력업체가 올린 자재
신청이 **다른 PC로 영영 안 갔다.** 결재는 여러 사람이 이어서 누르는 것이라
이대로면 성립하지 않는다.
★자재는 **공종코드(key)가 없다.** 갈래를 `if (!r.key) return null;`보다
**위에** 둘 것. 밑에 두면 전부 걸러진다.

## 5-C. 위치
```
부지토목 : {s:'civil', p:1~6, c:1~2}     Phase 3-1
부대토목 : {s:'anc', t:'A'~'H', b:1~9}   Town B · Block 7
```

---

# 6. 서버 (Apps Script)

## 6-A. 지금 붙어 있는 것 — v4
```
doPost           저장. 같은 id면 그 줄을 덮어쓴다(upsert) ★확인됨
?action=rows     조회. type·since·dateFrom·dateTo
?action=meta     마지막 수신시각만 (증분 수신용)
{type:'login'}   비밀번호 대조 (스크립트 속성 PW_STAFF·PW_ADMIN·PW_SURVEY)
```
★**v2.20.0에서 `PW_SURVEY`(측량팀)를 넣었다. 재배포가 필요하다.**
스크립트 속성에 등록하지 않으면 측량팀은 비밀번호가 없으므로 못 들어온다
— 나머지 화면은 종전대로 동작한다(막혀도 다른 것이 안 죽는다).
배포는 **[배포 관리] → 수정 → 새 버전 → 배포**다. [새 배포]를 누르면
주소가 바뀌어 현장이 쓰던 링크가 죽는다.

## 6-B. 증분 수신 — v2.19.3
```
처음      → 전체
그 뒤로   → meta로 「바뀐 게 있나」만 물음(수십 바이트)
            없으면 본문 요청 자체를 안 보냄
            있으면 since 이후 것만
```
★기준 시각은 **서버가 준 것**(`d.last`)을 쓴다. 내 시계를 쓰면 어긋난 사이의
줄을 영영 건너뛴다.
★**[전체 다시 받기]** 단추가 따로 있다. 증분만으로는 90일 정리로 덜어낸 것이나
저장소를 비운 뒤가 빈 화면으로 남는다.

## 6-C. 확인 방법
```
웹앱주소?action=meta  →  {"ok":true,"last":"…","count":31}
```
`{"ok":true,"msg":"BNCP API alive"}` 가 나오면 **배포가 안 된 것.**

---

# 7. 용량

- `localStorage` 한도 보통 **5MB**. 하루 100건이면 1~2년에 찬다.
- 화면은 **최근 90일**만 들고 있다. 원본은 서버에 있다.
- ★**미처리 건은 오래돼도 남긴다.** 밀린 일이 사라지면 안 된다.
- ★기준 자료(설계량·명부·마스터)는 안 덜어낸다.
- `A.save()`가 터지면 **그 자리에서 덜어내고 한 번 더 시도**한다.
- 덜어냈으면 **반드시 알린다**(`S.trimMsg`). 조용히 지우면 「내 자료가 왜 없지」가 된다.
- 준비 › **저장 용량**에서 사용량·직접 덜어내기.

★**근본 해결은 아직이다.** IndexedDB 전환 + 과거 조회 화면이 남았다.
지금은 90일 자르기로 버티는 중이다.

---

# 8. 협력업체 화면 (`vendor.html`)

- 접속 : `vendor.html?v=업체키` — 명부에 등록된 업체만
- 표기 : **영어 + 아랍어 병기**. 한국어가 보이면 안 된다(smoke가 검사)
- ★**값은 한국어 원문**, 보이는 글자만 번역
- 자재신청 탭은 **살아 있다** (v2.17.x에서 감췄던 것을 되돌림)
- ★**반려된 실적은 날짜와 무관하게 맨 위에** 뜬다. 어제 올린 것을 오늘 반려하면
  「오늘 것」에 안 걸려 영영 못 보기 때문이다.
- ★**공종을 고르면 담당자가 자동으로** 뜬다(명부의 `공종|이름`).
  명부에 담당자가 없으면 종전처럼 자유 입력 — 안 채운 현장이 막히면 안 된다.

★검사 주의 : 탭 전환은 `V.state.tab`이다. **`V.tab`에 넣으면 아무 일도
일어나지 않는다.** 그래서 검사가 다섯 탭을 돈다면서 첫 탭만 보고 통과한 적이 있다.

---

# 9. 독촉 문자

| 대상 | 언제 |
|---|---|
| 협력업체 미입력 | 마감 **08:00** 지나면 **08:30 · 09:00** 두 번 |
| 스탭 미확인 | 검측·측량 요청 뒤 **30분 · 60분** 두 번 |
| **반려 건** | **마감과 무관하게 바로** — 고쳐 올려야 하는데 모를 수 있다 |
| ★**결재가 멈춘 건** | 한 단계에서 **60분 · 120분** 두 번 (v2.20.0) |

★**화면과 문자 둘 다다**(사용자 지시 「화면하고 문자로 독촉하는 거야」).
- 화면 : `mineCard()` 위 경고 줄 + 늦은 줄 주황(`.wmark`) + 단계 배지 빨강
- 문자 : 탭5 독촉 카드에 붙는다. `A.flowDue(f, now)`가 대상을 뽑는다.
- ★문안은 **역할 앞**이다 — 사람 이름이 아니라 「관리자」·「측량팀」.
  결재는 자리에 걸린 일이지 특정 개인에게 걸린 일이 아니다.
- ★**업체 차례(back)는 여기서 안 센다.** `A.dueList`가 이미 맡고 있다 —
  두 곳에서 세면 두 번 간다.

- `A.dueList(f, now)` · `A.DUE = {hour:8, gap:30}`
- `A.flowDue(f, now)` · `A.DUE_FLOW = {gap:60}`
- 문안은 **업체 언어**를 따른다. 화면 언어와 별개다.
- 왓츠앱은 `wa.me` 링크 — **사람이 눌러야 열린다.**
- ★**무인 발송은 아직 안 된다.** WhatsApp Business(Cloud API) 계약이 필요하다.
  전용 번호(개인 번호 불가) · 템플릿 사전승인(자유 문장 불가) · 건당 과금.
  ★대상 추출이 `A.dueList`에서 끝나 있어 **서버는 그 결과만 가져다 쓰면 된다.**

---

# 10. 지금까지 고친 것 — 되돌리지 말 것

각 항목마다 검사가 붙어 있다. 되돌리면 즉시 걸린다.

| 버전 | 무엇 |
|---|---|
| 2.16.2 | 함수 중복(`eqHTML`) · 내역서 위치 어긋남 · [제외] 시 선택 초기화 · 5:2 배치 |
| 2.16.3 | 상단 KPI 4개 삭제 · 명칭 · 드롭다운 화살표 겹침 |
| 2.16.5 | 장비표 지급·회수·보유 (줄이 안 늘어나게) |
| 2.16.6 | ★대분류 펼치기 — `[data-ro]` 처리기가 **아예 없었다** |
| 2.16.7 | 기간 선택 단순화 (지름길 4 + 직접) |
| 2.16.8 | 지급대장 CSV UI 복구(코드는 살아 있었다) · 설계량 확인 목록 |
| 2.16.9 | 직영 권한(입력=스탭) · 작업량에 위치 표시 |
| 2.17.x | **전량 폐기** — 임의 통합 |
| 2.18.0 | 현황판 철거 → 요약 띠 + 공구 표 |
| 2.18.2 | **파일명으로 위치 판별** (P3-1·B-7·B7BL…) · 못 정하면 묻는다 |
| 2.18.4 | 설계량 서버 저장 (종전엔 브라우저에만 있었다) |
| 2.18.5 | 저장 용량 (90일·자동 덜어내기·알림) |
| 2.18.6 | ★**확인한 것이 되살아나던 문제** — 처리가 서버로 안 갔다 |
| 2.18.7 | 되돌려 보낼 때 공종명·단위 누락 |
| 2.18.8 | ★**자재 신청이 화면에서 사라짐**(`A.matRows` 중복 정의) · 반려 단추 |
| 2.18.9 | 실적=어제 / 투입=오늘 · 오늘+미처리 |
| 2.19.0 | 요약 띠에 「손봐야 할 것」 · 검측 날짜조회 · 설계 외 자재 |
| 2.19.1 | ★**자재 신청을 처리할 길이 없었다** — 지급·미지급(사유) |
| 2.19.2 | 공종 직접 추가 · 공종별 담당자 |
| 2.19.3 | 증분 수신 |
| 2.19.4 | ★**확인필요 67개가 저장돼 있어도 화면에 안 뜸**(boqHere가 옛 화면변수와 대조) |
| 2.19.5 | 자동매칭 규칙 3개 — 대분류 해제 · near 제외 · 느슨한 별칭(다른 페이즈에도 붙는다) |
| 2.19.6 | ★**대분류 파싱이 뿌리** — 이름을 안 보고 번호만 보고 올렸다. near의 자리 뺏기도 차단 |
| 2.19.7 | ★**고쳐도 화면은 옛 목록 그대로** — 지우기 단추가 없었다 + script 캐시 버전 누락 |
| 2.19.8 | 부대토목에 없는 것은 부지토목에서 (한 방향). 「622개 모자람」 기록 정정 |
| 2.19.9 | ★**요약 띠가 공구 표 머리행을 덮던 것** — sticky 걷어냄(갈래 가). 띠의 장비도 오늘 기준 |
| 2.19.10 | 확인필요 카드를 요약 띠 아래로 · 공구 표 열 너비(작업위치가 남는 폭을 받는다) |
| 2.19.11 | 「직영 작업 기록」→**직영현황** · 기본 오늘 · 기간 단추. ★[D6] 거짓 통과 정정 |
| 2.19.12 | ★**지급장비 파일이 아예 안 읽히던 것**(가로 표) · ★**업체 인원장비 폼 날짜가 어제**였다 |
| 2.19.13 | 작업량·작업위치도 **오늘**로 통일 · 업체 실적 폼도 오늘. 진행률은 누계 유지 |
| 2.21.1 | ★**[초기화]가 저장소를 통째로 지우던 것** — 협력업체 입력 5종만 · 관리자 비밀번호 관문 |
| 2.22.0 | ★**같은 id가 종류를 넘어 한 줄로 뭉개지던 것**(v4 서버) — 시트·캐시를 종류별로 가름 |
| 2.22.0 | ★**저장소의 `AppsScript_Code.gs`가 3단계에 멈춰 있던 것** — 올리면 측량팀 로그인이 사라진다 |
| 2.22.1 | ★**끝난 측량·자재가 기간 단추를 눌러도 안 나오던 것** — 같은 뜻 함수가 둘, 한쪽만 고쳐져 있었다 |
| 2.22.2 | ★**협력업체가 올린 플랜트 자재가 화면 어디에도 안 나오던 것** — 옛 화면을 끄며 갈래가 죽음 |
| 2.22.2 | ★**측량에 기간 단추가 없어 어제 완료건을 볼 길이 없던 것** |

---

# 11. 작업 순서

```bash
cd /home/claude/work && unzip -o -q <업로드된 zip> -d .
# 고치기
node tools/smoke.js && node tools/smoke_vendor.js && node tools/smoke_direct.js
for f in assets/js/*.js; do node --check "$f"; done
# 버전 올리기 : assets/js/version.js + VERSION 맨 위에 항목 추가
# 묶기 : outputs 에 zip
```

★VERSION에는 **무엇을 왜 고쳤는지, 원인이 무엇이었는지**를 적는다.
다음 사람이 같은 자리를 다시 파지 않도록.

---

# 12. 다음 창에서 「다음 시작해」 하면

0-A(2.19.4) · 0-B(2.19.6) · 0-B-2(2.19.7) · 0-E-2(2.19.8) ·
0-D · 0-D-2(2.19.9) · 0-D-3(2.19.10) · 직영현황(2.19.11) ·
0-C · 0-H(2.19.12) · 기간 통일(2.19.13) 완료.
0-D·0-D-2는 **화면에서도 확인됐다.**

**남은 것 — 순서대로**
0. ★**0-Z-3 구글 시트를 종류별로 가른다** — 여기부터다(2순위였던 것).
   0-Z-1·0-Z-2는 **v2.21.1에서 끝났다.** 0-Z 장을 먼저 읽을 것.
   - 0-Z-3 구글 시트 종류별 가르기 — ★`Code.gs`를 고치므로 **재배포가 따른다**
   - ★**0-Z-2-B 테스트 자료 일괄 삭제는 미결이다** — 사용자가 「나중에 생각하자」로
     보류했다. 먼저 꺼내지 말 것. 사용자가 물으면 0-Z-2-B를 읽고 답할 것.
   - 0-Z-4 용량 80% 경고·파일 자동 추가 (나중 · 0-Z-3 다음)
1. ★**0-I 장비 보유를 업체 축으로** — 사용자가 확정했고 아직 착수 전이다.
   0-I 장을 먼저 읽을 것.
2. **v2.19.13 눈으로 확인** — 작업량·작업위치가 오늘로 뜨는지,
   업체 화면 입력 폼 날짜가 넷 다 오늘인지.
4. **0-F** — [목록 지우기] 후 다시 올리면 확인필요가 15건이 되는지.
5. **0-E-1 시설물** — ★사용자 확인 필요. 아래 경고를 반드시 읽을 것.
6. **0-E-3 갤러리 누락 11행** — 앞 4개(토공 터파기·되메우기·잔토, 철근 H32)를
   `원단위_토공` 시트에서 끌어올지. ★사용자 확인 필요.

★**0-E-1을 다시 꺼낼 때 주의.** 생산성·매핑표는 **이미 완료된 것이다.**
매핑표 235건이 앱과 정확히 일치하는 것을 확인했다. 그런데 나는 그것을
확인해 놓고도 몇 턴 뒤에 「시설물 590개가 생산성이 없다」고 말했다.
공종마스터 파일의 **다른 시트(622행)** 를 원단위 행렬에 억지로 맞춰 본 숫자였다.
**축이 다른 두 자료를 비교한 것이라 그 숫자는 의미가 없다.**
사용자가 「전에 확인한 거잖아」라고 바로잡아 주셨다.
→ 완료된 것을 다시 흔들지 말 것. 의심이 들면 **먼저 이 문서를 볼 것.**

★**착수 전에 2장(일하는 방식)과 3장(반드시 지킬 규칙)을 읽을 것.**
★**진단을 문서만 믿고 착수하지 말 것.** 0-B는 인수인계서의 진단이 틀렸고,
실제 파일을 뜯어 보고서야 파서가 뿌리인 것을 찾았다. 숫자를 직접 재현할 것.
★**화면 숫자가 이상하면 저장된 옛 값부터 의심할 것** — 0-B-2도 0-F도 그것이었다.
로직은 맞는데 저장분과 브라우저 캐시 때문에 화면만 옛것인 경우가 반복됐다.
★**고친 뒤에는 화면에서 확인되는지까지 볼 것.** 시각적 판단은 사용자 몫이다(2-C).
★**대화가 길어지면 스스로 말할 것.** 작업 공간이 차면 고쳐 놓고 검사를
제대로 못 돌린다. 그 상태로 밀어붙이지 말고 새 창을 권할 것.
